# Database Design

The starter application uses a simple `Record` table with the following columns:

- `id`: primary key
- `name`: required string
- `description`: required text

This minimal schema is intended to be replaced with domain-specific tables as the application evolves.
