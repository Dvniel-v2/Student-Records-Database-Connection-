# MySQL Archive

This folder is reserved for the original MySQL database scripts.

Those files are retained only for migration history, comparison, reference and
academic development evidence. They must not be used by the Flask runtime.

The application stack remains Flask, SQLAlchemy, psycopg and PostgreSQL. Do not
add MySQL drivers, MySQL connection strings or MySQL-specific application logic.
